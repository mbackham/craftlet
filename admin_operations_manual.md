# 运营与风控后台操作手册

本文档为基于 Craftlet 后台（ActiveAdmin）的标准化操作指引，主要面向**运营团队**、**风控团队**及**系统管理员**。

---

## 1. 角色与权限划分

系统采用基于角色的访问控制（RBAC），不同角色的菜单可见性和操作权限受严格管控。

- **运营专员 (Ops)**：负责日常工单处理、订单及支付查询、普通客诉退款审批。
- **风控专员 (Risk)**：负责高危操作拦截、风控事件审核、商户入网审核及风控规则配置。
- **超级管理员 (Superadmin)**：拥有系统全量权限，额外负责员工账号分配及权限管理。

**安全提示**：
1. **敏感信息脱敏**：除特定提权角色外，所有用户的手机号、邮箱、银行卡等信息在列表中均显示为掩码。底表真实数据受最高级保护。
2. **全面审计 (Audit Logs)**：管理后台的任何核心写操作（审批、拒绝、指派、修改配置等）都会被记录到系统的「操作审计日志」中，包含操作人、时间、IP 及详细变更 payload。

---

## 2. 运营团队 (Ops) 操作指南

### 2.1 工单管理 (Tickets)
- **入口**：`订单管理` -> `工单管理`
- **新建工单**：在订单详情页可一键“创建退款工单”，或在工单列表手动新建，补充主题与描述。
- **指派与接单**：
  - 点击工单详情的 **“指派处理人”**，将工单分配给指定同事。系统通过防并发锁保证不会出现抢单冲突。
  - 接单后可点击 **“开始处理”**，工单状态将变更为 `处理中`。
- **回复与关闭**：
  - 此处支持与用户进行站内沟通或记录 **“内部备注”**（内部备注仅后台可见）。
  - 处理完毕后，点击 **“标记解决”** 或直接 **“关闭工单”**。

### 2.2 退款审批 (Refunds)
- **入口**：`订单管理` -> `退款管理`
- **操作规则**：
  - 列表页面显示各退款申请的来源订单、支付单号及申请退款金额。
  - 对于状态为 `init`（待审核）的退款，点击 **“审批通过”** 或 **“拒绝退款”**。
  - **防连击保护**：一旦点击提交按钮，按钮会立即锁定并提示“处理中...”，请勿刷新。
  - **异常重试**：如果退款网关报错（如余额不足、网络超时），退款状态将变为 `failed`，并展示具体的“第三方响应代码”。若是暂时性网络故障，可点击 **“重试退款”** 重新发起。
- **关联信息**：操作前请务必核对底部的“关联退款工单”，确认此前与用户的沟通进度。

---

## 3. 风控团队 (Risk) 操作指南

### 3.1 风险事件处理 (Risk Events)
- **入口**：`风控中心` -> `风险事件`
- **规则触发**：所有的风险事件由底层的 `Risk::Engine` 自动计算并生成（如：单日退款超限、异地登录、羊毛党高频下单等）。
- **人工干预**：
  - 状态为 `pending` 的事件需人工介入研判。
  - 若确认为欺诈行为，点击 **“确认风险（Confirm）”**，系统可自动联动挂起相关商户或冻结资金。
  - 若为误报，可点击 **“标记误报（Resolve）”**，释放相应业务。

### 3.2 风控规则配置 (Risk Rules)
- **入口**：`风控中心` -> `风控配置`
- **调整阈值**：可针对不同维度动态调整拦截阈值及权重分。操作需格外谨慎，错误的配置可能导致大面积阻断正常用户交易。每一次配置变更都会被计入审计日志。

---

## 4. 管理员 (Admin) 操作指南

### 4.1 账号与权限管理
- **入口**：`系统管理` -> `后台用户` / `角色权限`
- **创建员工账号**：新增员工邮箱，并分配对应角色（如 `ops_viewer`, `risk_manager`）。
- **动态授权**：角色的底层权限可动态勾选。若角色被剥夺了某个模块的 `read` 权限，该模块将自动从左侧菜单栏隐藏。

### 4.2 审计日志追溯
- **入口**：`系统管理` -> `操作审计日志`
- **故障排查**：通过过滤“操作类型（Action）”和“目标资源（Target Type）”，可以精确定位某条数据被谁、在何时进行了修改，以及修改前后的 Diff。
